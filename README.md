# FloatYourBoat
Project for FSU's Database Class Spring 2026

## How to Run

1. Install [python](https://www.python.org/downloads/)
2. Run `pip install -r requirements.txt`
3. Run `python backend/app.py` to start the backend server